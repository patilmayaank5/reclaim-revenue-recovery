[project]
name = "reclaim-backend"
version = "0.1.0"
description = "Reclaim â€” Revenue Recovery Intelligence Engine (Backend)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["../tests/backend"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
